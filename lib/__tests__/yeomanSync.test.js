const assert = require('assert');
const yeomanSync = require('../index.js');

describe('yeomanSync', function () {
  it('has a test', function () {
    assert(false, 'yeomanSync should have a test');
  });
});
